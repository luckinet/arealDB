% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regVocabulary.R
\name{regVocabulary}
\alias{regVocabulary}
\title{Register a new vocabulary contributor}
\usage{
regVocabulary(
  name,
  dSeries,
  description,
  schema = NULL,
  archive = NULL,
  archiveLink = NULL,
  downloadDate = NULL,
  version = NULL,
  licence_link = NULL,
  notes = NULL,
  diagnose = FALSE,
  overwrite = FALSE
)
}
\arguments{
\item{name}{\code{\link[=character]{character(1)}}\cr name of the target vocabulary
(e.g. \code{"gazetteer"}, \code{"commodity"}).}

\item{dSeries}{\code{\link[=character]{character(1)}}\cr name of the contributing
dataseries (must already exist in \code{inv_dataseries}; see
\code{\link{regDataseries}}).}

\item{description}{\code{\link[=character]{character(1)}}\cr free-text description of
what the vocabulary represents.}

\item{schema}{\code{\link[tabshiftr:schema-class]{schema}}\cr the tabshiftr schema
describing how to reshape the contributor's stage 2 file. Must produce at
minimum the required columns for the role (see Details).}

\item{archive}{\code{\link[=character]{character(1)}}\cr the original (stage 1) file
from which the vocabulary entries originate.}

\item{archiveLink}{\code{\link[=character]{character(1)}}\cr download link of the
archive.}

\item{downloadDate}{\code{\link[=character]{character(1)}}\cr download date in
YYYY-MM-DD format.}

\item{version}{\code{\link[=character]{character(1)}}\cr version of the vocabulary.}

\item{licence_link}{\code{\link[=character]{character(1)}}\cr licence URL.}

\item{notes}{\code{\link[=character]{character(1)}}\cr optional notes.}

\item{diagnose}{\code{\link[=logical]{logical(1)}}\cr if TRUE, attempt
\code{\link[tabshiftr]{reorganise}} on the stage 2 file with the schema
to verify it produces the required columns.}

\item{overwrite}{\code{\link[=logical]{logical(1)}}\cr whether to overwrite an
existing registration of this contributor.}
}
\value{
Returns a tibble of the entry that is appended to
\code{inv_vocabularies}.
}
\description{
This function registers an external dataseries' vocabulary file as a
contributor to a named vocabulary (e.g. \code{gazetteer},
\code{commodity}). The first contributor to a given vocabulary becomes its
\emph{backbone} (defines the canonical terms and levels at normalisation
time). Subsequent contributors provide \emph{mappings} from their source
labels to canonical IDs of the existing backbone.
}
\details{
The role of the contributor is determined automatically at
normalisation time by \code{\link{normVocabulary}}: if the target
vocabulary's terms file does not yet exist, this contributor is the
backbone; otherwise it provides mappings.

Required columns the schema must produce:
\itemize{
\item Backbone (first contributor): \code{label}, \code{class},
\code{parent_label}. Optional: \code{description}, \code{valid_from},
\code{valid_until}, \code{successor_id}.
\item Mapping (subsequent contributors): \code{source_label}, plus any
additional source-specific columns (preserved through to the mappings
file).
}
}
\seealso{
Other register functions: 
\code{\link{regDataseries}()},
\code{\link{regGeometry}()},
\code{\link{regTable}()}
}
\concept{register functions}
