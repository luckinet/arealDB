% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match_builder.R
\name{match_builder}
\alias{match_builder}
\title{Interactively match unresolved terms to canonical vocabulary concepts}
\usage{
match_builder(
  new,
  topLevel = FALSE,
  source = NULL,
  ontology = NULL,
  stringdist = TRUE
)
}
\arguments{
\item{new}{\code{\link[=data.frame]{data.frame(.)}}\cr tibble of unmatched terms with
columns \code{source_label}, \code{parent_id}, \code{canonical_id} (NA),
\code{note} (NA).}

\item{topLevel}{\code{\link[=logical]{logical(1)}}\cr TRUE when matching the top
hierarchy level (no parent context available).}

\item{source}{\code{\link[=character]{character(1)}}\cr dataseries name, used as the
\code{source} column in the written mappings.}

\item{ontology}{\code{\link[=character]{character(1)}}\cr name of the vocabulary
to match against (e.g. \code{"gazetteer"}, \code{"commodity"}).}

\item{stringdist}{\code{\link[=logical]{logical(1)}}\cr whether to pre-fill fuzzy
candidate suggestions.}
}
\value{
tibble with columns \code{source_label}, \code{canonical_id},
\code{parent_id}, \code{note}. Only rows where the user confirmed a
\code{canonical_id} (or set note to "ignore"/"new") are returned.
}
\description{
Opens a Shiny app that presents unresolved source labels alongside fuzzy
candidates and the relevant parent context, allowing the user to confirm,
reject, or reassign each match. Confirmed assignments are written to the
vocabulary directory via \code{.write_mappings()} when the app closes.
}
\details{
Requires the \pkg{shiny} and \pkg{DT} packages.
}
\examples{
\dontrun{
match_builder(
  new       = tibble(source_label = c("Ain", "Alpes"),
                     parent_id    = c(".001", ".001"),
                     canonical_id = NA_character_,
                     note         = NA_character_),
  topLevel  = FALSE,
  source    = "eurostat",
  ontology  = "gazetteer"
)
}
}
